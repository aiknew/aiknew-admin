export {
  FileStorageStatus,
  StorageType,
  FileStatus,
  LanguageOrientation,
  RequestMethod,
  RouteType,
  UploadFileChannel,
  AdminPermissionSource,
} from "@aiknew/shared-admin-db/enums"

export const VerificationTypeEnum = {
  CAPTCHA: "CAPTCHA",
  ALTCHA: "ALTCHA",
} as const
